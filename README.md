# Mors Code Translator

Basit bir HTML/CSS/JS proje. GitHub'a yüklemek için aşağıdaki adımları kullanabilirsiniz:

1. Yerel repoyu başlat: `git init`
2. Dosyaları ekle: `git add .`
3. İlk commit: `git commit -m "Initial commit"`
4. GitHub'da repo oluşturup remote ekle ve `git push` yapın.
